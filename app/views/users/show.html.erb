<h1> Hey <%= @user.username %>! </h1>
<p>Here's a list of your reviews:</p>
    <div class="reviews">
    <% @user.reviews.each do |review| %>
        <div class="reviews_card">
        <h3><%= link_to review.establishment.name, review_path(review)%>⎮⎮ <%= link_to "Edit your review", edit_review_path(review)%></h3>
    </div>
        <% end %> 
    </div>

    
     
          

   